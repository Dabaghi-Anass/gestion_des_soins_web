import { Skeleton } from '@/components/ui/skeleton';
import React from 'react';
const NavSkeleton: React.FC = () => {
  return (
    <Skeleton className="w-screen h-12" />
  );
};

export default NavSkeleton;